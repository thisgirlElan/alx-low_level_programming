#include "main.h"
#include <stdio.h>

/**
* _abs - checks the absolute value of an integer
*@r: value being returned
*
* Return: the absolute value
*/

int _abs(int r)
{
if (r >= 0)
{
return (r);
}
else
{
return (-r);
}

}
